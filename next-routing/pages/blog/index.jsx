export default function Blog() {
  return <h1>Blog Pages</h1>;
}
//blog folder create a rout like /blog
// so we need root page so we named root comp to index.js again
// /blog/first and /blog/second we simply made two .js with route name
